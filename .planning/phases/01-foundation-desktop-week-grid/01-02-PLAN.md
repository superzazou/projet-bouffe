---
phase: 01-foundation-desktop-week-grid
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/(app)/planning/PlanningWeek.tsx
autonomous: false
requirements:
  - DSK-01
  - DSK-02
  - DSK-03
  - DSK-04
  - SHR-01
  - SHR-02
  - SHR-03
  - SHR-04
must_haves:
  truths:
    - "User sees all 7 day columns side by side in one horizontal row on desktop — grid grid-cols-7 min-w-[900px] (DSK-01)"
    - "Today column header shows a filled bg-stone-900 text-white circle behind the date number (D-01, DSK-02)"
    - "Today day abbreviation label has text-stone-900 font-semibold; other days have text-stone-400 (D-02, DSK-02)"
    - "Column body (meal slots area) has identical classes for today and other days — no today-specific background or border on the body (D-03)"
    - "Navigation row is a single flex justify-between row: shopping list button on the left, week nav controls on the right (D-04, D-06)"
    - "Week label shows Cette semaine when weekOffset equals 0; formatted range via formatWeekLabel otherwise (D-05)"
    - "Prev/next week navigation limited to plus/minus 4 weeks via canGoPrev/canGoNext (D-07, DSK-03)"
    - "Each day column contains Midi and Soir slots rendered via MEAL_TYPES map (SHR-01)"
    - "RecipeCombobox rendered in each slot with onAdd bound to handleAdd(dateStr, mealType, id) (SHR-02)"
    - "Retirer button in each recipe row calls handleRemove(plan.id) (SHR-03)"
    - "Creer une liste de courses button is in the nav row; disabled when upcomingRecipes.length is 0; opens modal when clicked (SHR-04)"
    - "Scroll wrapper uses only overflow-x-auto; grid container and column divs use default visible overflow to avoid clipping the RecipeCombobox absolute dropdown"
  artifacts:
    - src/app/(app)/planning/PlanningWeek.tsx
  key_links:
    - "overflow-x-auto wrapper → grid min-w-[900px] — prevents layout collapse on sub-900px viewports without clipping dropdowns"
    - "isToday → bg-stone-900 text-white badge on date span; text-stone-900 font-semibold on abbrev — today visual distinction"
    - "shopping list button disabled when upcomingRecipes.length === 0 — existing functional guard preserved"
    - "RecipeCombobox excludeIds=addedRecipeIds — prevents adding a recipe already in the slot"
  prohibitions:
    - statement: "Grid container div and direct day-column divs must not carry any overflow-clipping Tailwind class (would hide RecipeCombobox absolute dropdown)"
      status: prohibited
      verification: "No Tailwind overflow-clipping class on the grid div (grid grid-cols-7 element) or its direct day-column children"
    - statement: "Column body div (meal slots container) must not apply today-specific background or border styling (D-03)"
      status: prohibited
      verification: "Meal slots container div inside each column has identical className regardless of isToday value"
    - statement: "No external calendar library may be imported"
      status: prohibited
      verification: "No import statement in PlanningWeek.tsx references an external calendar package"
---

<objective>
Replace the vertical flex-col day-card list in PlanningWeek.tsx with a 7-column horizontal grid layout. Add column headers with day abbreviation and today badge per D-01/D-02/D-04. Merge the shopping list button into the navigation row per D-06. Preserve all existing slot interactions: recipe add, recipe remove, week navigation, shopping list modal.

Purpose: Deliver the Google Calendar-style desktop week view so the user can see and manage all 7 days at a glance.
Output: PlanningWeek.tsx with grid grid-cols-7 layout, today badge in column headers, merged nav row, and all slot interactions preserved. Human smoke-test checkpoint confirms end-to-end behavior.
</objective>

<execution_context>
@/Users/vincent.dumont/code/projet-bouffe/sourcecode/.claude/gsd-core/workflows/execute-plan.md
@/Users/vincent.dumont/code/projet-bouffe/sourcecode/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-desktop-week-grid/01-CONTEXT.md
@.planning/phases/01-foundation-desktop-week-grid/01-RESEARCH.md
@.planning/phases/01-foundation-desktop-week-grid/01-PATTERNS.md
@.planning/phases/01-foundation-desktop-week-grid/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace vertical layout with 7-column horizontal grid and merge navigation row (D-01 through D-07, SHR-01 through SHR-04)</name>
  <files>src/app/(app)/planning/PlanningWeek.tsx</files>
  <read_first>
    - src/app/(app)/planning/PlanningWeek.tsx — read the full file after Plan 01-01 changes; focus on the return JSX (the outer flex container and all children)
    - .planning/phases/01-foundation-desktop-week-grid/01-RESEARCH.md — Pattern 4 (7-column CSS grid layout with overflow-x-auto wrapper), Pattern 5 (navigation row with shopping list button per D-06), Pitfall 1 (min-w guard), Pitfall 2 (dropdown visibility and scroll wrapper rules)
    - .planning/phases/01-foundation-desktop-week-grid/01-PATTERNS.md — grid layout pattern block, navigation row pattern block, stone palette shared patterns
    - .planning/phases/01-foundation-desktop-week-grid/01-CONTEXT.md — decisions D-01 through D-07 (locked decisions, non-negotiable)
  </read_first>
  <action>
    Remove the standalone div that wraps only the "Créer une liste de courses" button (currently the first child of the outer flex flex-col container). This button moves into the navigation row per D-06.

    Rewrite the navigation row: change the existing div to flex items-center justify-between gap-4 flex-wrap. As its first child, place the "Créer une liste de courses" button with its existing disabled logic, click handler (openListModal), and existing Tailwind classes. As its second child, place a div with flex items-center gap-4 containing the prev-week button, the week label span, and the next-week button — all with identical logic and Tailwind classes to the current navigation row. This satisfies D-04 (dedicated nav row above the grid) and D-06 (shopping list button left, nav right in same row).

    Replace the vertical day list (the div with flex flex-col gap-3 containing the day-card divs) with a horizontal grid. The outer wrapper div uses overflow-x-auto only — use no other overflow modifier on this element or any descendant column element, so the RecipeCombobox absolute dropdown is not clipped. The inner grid div uses grid grid-cols-7 min-w-[900px] border border-stone-200 rounded-lg divide-x divide-stone-200.

    Each day in the days.map becomes a flex flex-col div (the column). The column header is a div with flex flex-col items-center gap-1 py-2 border-b border-stone-200, containing:
    - A span for the day abbreviation (DAY_LABELS[i]): apply text-xs font-medium plus text-stone-900 font-semibold when isToday, text-stone-400 otherwise. This implements D-02.
    - A span for the date number (day.getDate()): apply w-7 h-7 flex items-center justify-center rounded-full text-sm font-medium plus bg-stone-900 text-white when isToday, text-stone-700 otherwise. This implements D-01.

    The column body is a div with flex flex-col gap-3 p-2. This div must have identical className whether isToday is true or false — no conditional class on this element. This implements D-03.

    Inside the column body, map MEAL_TYPES to render each slot as a flex flex-col gap-1.5 div. Each slot contains: a meal label span (text-xs text-stone-400 with MEAL_LABELS[mealType]), the list of recipe rows (Link to /recipes/{recipe.id} with flex-1 truncate styling + a Retirer button calling handleRemove(plan.id) — identical to the existing recipe row markup), and a RecipeCombobox with props recipes={recipes}, onAdd={(id) => handleAdd(dateStr, mealType, id)}, disabled={isSaving}, excludeIds={addedRecipeIds}. The slot key derivation, getSlotPlans call, isSaving check, and addedRecipeIds derivation are identical to the existing column logic.

    Keep the shopping list modal JSX (showListModal block) entirely unchanged. Keep all function bodies (handleAdd, handleRemove, handleCreateList, openListModal, getSlotPlans), all useState declarations, all imports, and the "use client" directive unchanged.
  </action>
  <acceptance_criteria>
    - PlanningWeek.tsx contains grid grid-cols-7 and min-w-[900px] in the grid container div
    - PlanningWeek.tsx contains overflow-x-auto on the outer wrapper div that contains the grid
    - Column header span for the date number contains bg-stone-900 text-white when isToday — confirms D-01 badge
    - Column header span for the day abbreviation contains text-stone-900 font-semibold when isToday — confirms D-02
    - The meal slots container div inside each column has a single fixed className (no ternary or conditional on that div) — confirms D-03
    - "Créer une liste de courses" button and the three navigation elements are siblings inside a single justify-between flex row — confirms D-06
    - npm run build exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/vincent.dumont/code/projet-bouffe/sourcecode && npm run build && npm run lint</automated>
  </verify>
  <done>PlanningWeek.tsx renders a 7-column horizontal grid. Today column has the date badge. Shopping list button is in the navigation row. All slot interactions (add, remove, shopping list modal) are preserved. Build and lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Smoke test: verify 7-column grid, today badge, navigation, and all slot interactions</name>
  <what-built>PlanningWeek.tsx rewritten as a 7-column horizontal grid (DSK-01) with today badge header (DSK-02), day abbreviation + date number columns (DSK-04), week navigation (DSK-03), Midi/Soir slots with inline combobox add and Retirer remove (SHR-01, SHR-02, SHR-03), and shopping list button merged into the navigation row (SHR-04).</what-built>
  <how-to-verify>
    Run the dev server with: npm run dev
    Then open http://localhost:3000/planning (log in if prompted).

    1. Layout — DSK-01: Confirm 7 day columns display side by side in one horizontal row. No vertical stacking of day cards.
    2. Today badge — DSK-02, D-01: Confirm today's column header shows a filled dark circle around the date number. Other columns show no circle.
    3. Today abbreviation — D-02: Confirm today's abbreviated day name (e.g. Lun, Mar) is bold and dark. Other days are lighter.
    4. Column body parity — D-03: Confirm today's column body (the meal slots area below the header) looks identical to other days. No extra background or border.
    5. Column headers — DSK-04: Confirm each column shows the abbreviated day name above the date number.
    6. Navigation row — D-06: Confirm "Créer une liste de courses" and "← Semaine précédente / Semaine suivante →" are in the same horizontal row. No separate row for the shopping list button.
    7. Navigation — DSK-03, D-05: Click "← Semaine précédente". Columns shift back 7 days and the week label shows the formatted range. Click "Semaine suivante →" until you return to the current week and confirm "Cette semaine" label appears.
    8. Navigation limit — D-07: Confirm the prev button is disabled after 4 clicks back; the next button is disabled after 4 clicks forward from the current week.
    9. Slots — SHR-01: Confirm each column shows a Midi slot and a Soir slot.
    10. Add recipe — SHR-02: Click the combobox in any slot, type a recipe name, select a result. Confirm the recipe appears in the slot immediately without a page reload.
    11. Remove recipe — SHR-03: Click "Retirer" next to any recipe in a slot. Confirm it disappears immediately without a page reload.
    12. Shopping list — SHR-04: Confirm "Créer une liste de courses" is disabled when no upcoming meals are planned. When meals are planned, click it: confirm the modal opens with a recipe checklist, entering a list name and clicking "Créer" navigates to the new shopping list.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 checks pass. If any check fails, describe the issue so it can be fixed before closing the phase.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → RecipeCombobox → handleAdd | Recipe selection passes a recipeId string to server action addMealPlan — auth guard in actions.ts unchanged |
| User input → Retirer button → handleRemove | mealPlanId passed to server action deleteMealPlan — auth and ownership guard in actions.ts unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | handleAdd / addMealPlan | low | accept | addMealPlan validates auth via getUser() server-side — no new input surface introduced by the layout change |
| T-02-02 | Tampering | handleRemove / deleteMealPlan | low | accept | deleteMealPlan validates auth and scopes deletion to the authenticated user's data — unchanged |
| T-02-03 | Information Disclosure | RecipeCombobox recipe list | low | accept | Recipe list is scoped to the authenticated user's data fetched server-side in page.tsx — no new exposure |
</threat_model>

<verification>
npm run build passes — TypeScript validates the grid layout and all component interactions.
npm run lint passes — ESLint confirms no style violations.
Human checkpoint (Task 2 above): all 12 checklist items confirmed by developer.
</verification>

<success_criteria>
Phase 1 complete. All ROADMAP Phase 1 success criteria met:
1. User sees 7 day columns side by side with today's column header visually highlighted — DSK-01, DSK-02 (FND-01 provides correct dates)
2. User can navigate to prev/next week via arrow controls; today is the starting point on page load — DSK-03
3. Each day column shows abbreviated day name and date number; dates correct regardless of UTC offset — DSK-04, FND-01
4. User can add a recipe to any Midi or Soir slot via inline combobox and see it appear immediately — SHR-01, SHR-02
5. User can remove a recipe; "Créer une liste de courses" button remains accessible and functional — SHR-03, SHR-04
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| getMondayOf | function (modified) | src/app/(app)/planning/PlanningWeek.tsx | Local-midnight parsing via split+3-arg constructor (Plan 01-01) |
| toDateStr | function (modified) | src/app/(app)/planning/PlanningWeek.tsx | Local-part formatting via getFullYear/getMonth/getDate (Plan 01-01) |
| todayStr | local constant (new) | src/app/(app)/planning/PlanningWeek.tsx | Client-derived local today string at render time (Plan 01-01) |
| PlanningWeek return JSX — grid layout | markup (modified) | src/app/(app)/planning/PlanningWeek.tsx | grid grid-cols-7 min-w-[900px] replacing flex flex-col gap-3 day cards |
| Column header | markup (new) | src/app/(app)/planning/PlanningWeek.tsx | Day abbrev span + date badge span per D-01/D-02/D-04 |
| Navigation row | markup (modified) | src/app/(app)/planning/PlanningWeek.tsx | Single justify-between flex row: shopping list button left + nav controls right (D-06) |

<output>
Create `.planning/phases/01-foundation-desktop-week-grid/01-02-SUMMARY.md` when done.
</output>
